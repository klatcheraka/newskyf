services:
  - type: web
    name: skyfreelanc-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 bot.py
    envVars:
      - key: TOKEN
        fromSecret: TOKEN
      - key: CHANNEL
        fromSecret: CHANNEL

