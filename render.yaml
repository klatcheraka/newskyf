services:
  - type: web
    name: skyfreelanc-bot
    runtime: python  # ✅ исправлено: было env
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 bot.py
    envVars:
      - key: TOKEN
        fromSecret: TOKEN
      - key: CHANNEL
        fromSecret: CHANNEL
